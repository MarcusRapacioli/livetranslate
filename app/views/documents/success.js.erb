console.log(<%= @id %>)
function success() {
      swal({
        title: "Congrats",
        text: "You've just returned the translation",
        icon: "success"
      }).then(function() {
        window.location = "/users/<%= @id %>"
      })
    }


success();
